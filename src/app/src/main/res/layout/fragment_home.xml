<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.spreadtracker.ui.pager.OverlayViewPager
        android:id="@+id/fragment_home_viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:layout_editor_absoluteX="-16dp"
        tools:layout_editor_absoluteY="-16dp" />

    <ImageView
        android:id="@+id/fragment_home_infoButton"
        android:layout_width="@dimen/activity_main_iconSize"
        android:layout_height="@dimen/activity_main_iconSize"
        android:src="@drawable/ic_info"
        android:tint="@color/grayDark"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <ImageView
        android:id="@+id/fragment_home_profileButton"
        android:layout_width="@dimen/activity_main_iconSize"
        android:layout_height="@dimen/activity_main_iconSize"
        android:src="@drawable/ic_profile"
        android:tint="@color/grayDark"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>